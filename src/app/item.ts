// This is the item order interface
//id = order id
//name = item name
//description = other order info (shipping address, shipping time, etc.)
export interface Item {
    id: number;
    name: string;
    description: string;
}